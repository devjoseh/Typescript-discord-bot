interface Config {
    token: string;
    guild_id: string | null;
    client_id: string;
    owner_id: string;
}

const config: Config = {
    token: "",

    guild_id: "",
    client_id: "",
    owner_id: ""
}

export default config