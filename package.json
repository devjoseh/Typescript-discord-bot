{
  "name": "typescript-discord-bot",
  "version": "1.0.0",
  "author": "DevJoseH",
  "license": "ISC",
  "description": "Um simples bot para o discord usando TypeScript",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:prod": "node dist/index.js",
    "start": "npm run build && npm run start:prod",
    "build": "tsc --pretty",
    "clean": "rimraf dist",
    "watch": "tsc -w",
    "deploy": "npm run build"
  },
  "directories": {
    "lib": "src"
  },
  "dependencies": {
    "@types/node": "^20.12.12",
    "chalk": "^2.4.1",
    "cli-table3": "^0.6.5",
    "discord.js": "^14.15.2",
    "moment": "^2.30.1",
    "ts-node": "^10.9.2"
  },
  "devDependencies": {
    "rimraf": "^5.0.7"
  }
}
